	int a[6] = { 10,7,15,20,3,1 };
	int i,max,num;
	max = a[0];
	num = 0;
	for (i = 0; i < 6; i++)
	{
		if (a[i] > max)//如果当前值大于max,赋值给max
			max = a[i];
			num = i;
	}
	printf("max=%d\n", max);
	printf("num is %d", num);
	return 0;
