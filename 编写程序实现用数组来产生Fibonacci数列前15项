	//Fibonacci数列为1,1,2,3,5,8,13,...
	//f(i)=f(i-1)+f(i-2),f(1)=1,f(2)=1(第一项和第二项为1，后面每一项都是前面两项之和)
	int f[15];
	int i;
	f[0] = 1, f[1] = 1;
	for (i = 2; i < 15; i++)//排除前两项f[0],f[1]
	{						
		f[i] = f[i - 1] + f[i - 2];//f[i]=前1项+前第2项相加
	}
	for (i = 0; i < 15; i++) 
	{
		printf("%d",f[i]);
	}
	return 0;
